module twoComp(
         input [2:0]num, 
			output [2:0]abs,
			output negative);
			
			always @(*) begin
			   if(num[2] == 1'b1) begin
				     negative == 1'b1;
					  abs = -num;
				end else begin
				     negative == 1'b0;
					  abs = num;
				end
		   end
endmodule
